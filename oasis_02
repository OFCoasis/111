
#include<stdio.h>
#include<string.h>
void main()
{
	int cent = 0, Q;
	char str[1000];
	gets(str);
	Q = strlen(str);
	printf("%d\n",Q);
	for (int i = 0; i <= Q; i++)
	{
		if (str[i] == 'i'||str[i]=='I')
		{
			if (str[i + 1] == 't')
				cent =cent+1;
		}
	}
	printf("这段英语语句中有%d个it。\n", cent);
}
